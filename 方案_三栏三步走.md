# 三栏三步走方案 — 王远峰论文路径落地

**来源（2026-08-14 王远峰原话）**：
1. "经典特征 +SVM 和经典特征通过量子 feature map 转换 + SVM, 再对比一下 quantum circuit 生成的特征（optionally + 经典特征）"
2. "就是基于 quantum kernel 的 embedding"

## 三栏定义（王远峰口径）

| 栏 | 输入特征 | 核 / 分类器 | 一句话 | 状态 |
|---|---|---|---|---|
| ① 经典特征 + SVM | 经典5：gap4/ee/dimer/s_occ/lam_half | 经典 RBF-SVM | 经典算特征 + 经典核 | ✅ 定稿（acc=100%、KTA=62.44%） |
| ② 经典特征 → 量子 feature map → SVM | 同 ① 的经典5 | **量子核** K(x,x′)=\|⟨φ(x)\|φ(x′)⟩\|² | 经典算特征 + **量子核**（"基于 quantum kernel 的 embedding"，Havlíček 2019 路线） | ❌ **新工作** |
| ③ quantum circuit 生成的特征（optionally + 经典） | DQAP 电路输出（fidelity / 态） | 经典 precomputed-SVM | **量子算特征** + 经典核 | 🔶 **取样进行** |

**栏② vs 栏③ 的本质区别**：② 是**量子算核**（输入还是经典5特征，只把相似度换成量子测的保真度）；③ 是**量子算特征**（DQAP 电路直接产出物理特征）。两栏正好覆盖"量子核"与"量子特征"两个方向。

## 与旧方案对比（2026-08-14，用户要求：先对比再动手）

**旧方案（qkm_ml.py 现状 + gap4 memory §kernel 融合设计）**：

| 栏 | 内容 |
|---|---|
| ① 经典-only | 经典5特征 + RBF-SVM |
| ② 量子-only | K_DQAP（DQAP 态 fidelity 核）→ precomputed-SVM |
| ③ 混合 | 核级融合 K_hyb(w)=w·K̂_Q+(1−w)·K̂_C，w CV 选 |

**新方案（王远峰口径）**：

| 栏 | 内容 | 与旧方案关系 |
|---|---|---|
| ① 经典特征+SVM | 经典5 + RBF-SVM | **不变** |
| ② 经典特征→量子 feature map→SVM | 经典5 → ZZFeatureMap 量子核 \|⟨φ(x)\|φ(x′)⟩\|² → SVM | **全新计算**（feature-map 线路，非 DQAP） |
| ③ 量子电路生成特征（optionally+经典） | DQAP 态 → fidelity/⟨Z⟩/⟨n⟩/corr + 经典核 SVM | **吸收旧②+旧③** |

**关键差异**：
1. **旧② → 新③ 的"读出"**：旧②把 K_DQAP 直接当核喂 SVM；新③里 DQAP 态到手后 fidelity 核/⟨Z⟩/⟨n⟩/关联矩阵全免费（"一个态多次读出"），K_DQAP 变成③的其中一个读出（③a=特征路线、③b=核路线，均免费）。
2. **新② 是全新计算**：经典5特征 → feature-map 浅线路 → 量子核。这就是王远峰说的"基于 quantum kernel 的 embedding"（Havlíček 2019 量子核估计器配方），旧方案完全没有这条。
3. **旧③ → 新③ 语义变化（须留意）**：旧③是**核级**融合 w·K̂_Q+(1−w)·K̂_C；新③ optional 是**特征级**拼接（DQAP 特征 ∥ 经典5）。都是"量子+经典"组合但层不同。**建议保留核级混合为③内部子诊断**（态到手后免费，不丢旧资产）。
4. **覆盖方向**：新方案比旧方案多覆盖"经典算特征 + 量子核"这个方向；旧方案的"量子核+经典核混合"由③子诊断承接。

**不变项**（纪律延续）：三源独立（OBC 特征 / PBC-TBC 标签 / 量子态），只共享 (t1,t2,U)；单粒子不变量（zak/winding）排除；ee=100% 锁死诚实 framing。

## 三步走（顺序执行，每步独立可交付）

```
Step 1（已完成）→ Step 2（新，全网格便宜）→ Step 3（取样，贵）
```

- Step 2 不依赖 DQAP 态，只用经典特征 + feature map，可立即开工
- Step 3 需 DQAP 逐点优化（主成本），尽早启动、多核并行

---

### Step 1：经典特征 + SVM（栏①）— ✅ 已完成
- `baseline_ml.py --feat classical5`：RBF-SVM **acc=100%**（CI=[99.6,100]）、**KTA=62.44%**、null 51.4%
- 单特征诊断：**ee 独自 100%**（ES 熵≈完整序参量，triv0.45/topo1.90）；s_occ 死特征（KTA 4.1%）候选删；dimer 非无用（KTA 72.7%）保留
- 报告 4 图 + `report_classical5/report_classical5.md` 已交王远峰
- **栏仍 = gap4=69.5%**（LogReg 参照线）；classical5 = 完整信息上界参照，**非** QKM 要跨的栏
- 已封顶 100% → Step 2/3 的 claim 锁死 = "量子携带同等定相信息 / 可硬件化"，**非赢过经典**

---

### Step 2：经典特征 → 量子 feature map → SVM（栏②）— ❌ 新工作，全网格

**做什么**：把经典5特征 x 编码成 |φ(x)⟩（feature-map 浅线路），量子核 K(x,x′)=|⟨φ(x)|φ(x′)⟩|² 替代经典 RBF 核，喂 precomputed-SVM。**这就是王远峰要的"基于 quantum kernel 的 embedding"，配方 = Havlíček 2019 的 quantum kernel estimator**（Nature 567:209，经典 SVM + 量子核估计器）。

**为什么全网格可行（关键成本判断）**：feature map 是浅小线路（5 特征 ≈ 5 qubit），statevector 模拟器上 n=1092 点只需 **n 次线路求值**；kernel 矩阵 = 两态向量内积的模平方 = **O(n²) 免费内积**。成本量级 ~ 秒级，远低于 DQAP（栏③ ~1.4hr/点）。

**Feature map 候选**：
- 基线：Qiskit `ZZFeatureMap`（Havlíček 2019 标准，5 qubit，2 阶编码，linear entangler）
- 对照：Angle encoding（1 阶 RX/RY 逐特征）→ 看 1st vs 2nd order 对相变信息的影响
- 编码细节：特征 min-max 缩放到 [0, π]（ZZFeatureMap 把特征当旋转角，需有界；StandardScaler 输出无界会 wrap）

**数据协议**（无污染，沿用现有纪律）：
- 输入 x：`build_features_grid`（ssh_dataset_L8_labelgrid.npz，OBC ED）
- 标签 y：`topo_dataset_full.npz` γ_up（PBC/TBC），唯一标签来源
- SVM：precomputed kernel，5-fold CV，C 训练折内选（复用 `kernel_ml_utils.tune_cv_precomputed`）
- 核计算：exact statevector（理想器件极限）→ 天然 PSD，无 shot noise

**诊断**（复用 `kernel_ml_utils` + 新增文献级诊断）：
- PSD：min_eig ≥ −1e-9
- KTA：K_Q(embedding) vs K_C(RBF) 及逐特征 → 量子核是否真带相变信息
- **geometric difference（Huang et al. NC 12:2631, 2021）**：定量筛子——几何差小 ⇒ 经典核保证能匹配量子核，与标签无关。对 5-qubit 浅编码预期小，这就是"为什么没有优势空间"的严格证据
- **核平凡化检查（Thanasilp et al. NC 15:5200, 2024）**：看 Gram 对角/非对角结构是否接近 identity/随机（过度表达性会浓度化）。5 qubit 浅线路预期安全，但作为 sanity check 便宜
- 消融：**② vs ①**（量子核相对经典核的增量）；**② vs ③**（embedding vs 生成特征）
- 决策图：预测画回 (t1,t2) 逐 U 层，边界应贴 t1=t2 线

**诚实 framing（调研核实）**：
- Huang, Kueng, Preskill, PRL 126:190505 (2021)：经典数据 + 平均预测误差 → **量子无指数优势**（信息论界）；指数分离只在"学习量子数据/最坏情形 Pauli 预测"出现。我们的特征全是经典的 → 预期 ②≈①。
- Bowles, Ahmed, Schuld, arXiv:2403.07059 (2024)：12 种 QML 模型 × 160 数据集 × 10 benchmark，**开箱经典 baseline 系统性胜出**；55 篇文献样本 ~40% 声称优势 = 系统性正向偏倚。
- 结论：**论文 claim 锁死 = "量子核携带同等定相信息 + geometric difference/KTA 量化依据 + 可硬件化"，不是量子赢经典**。这本身（含"为什么没有优势空间"的严格量化）是可发表的诚实结果。SSH 拓扑相分类用量子核 = **文献空白**（检索未见直接先例，见下方调研备注）。

**落地**：新脚本 `qkm_featuremap.py`，复用 `build_features_grid` + `kernel_ml_utils`；geometric difference 需新增小函数（Huang 2021 定义）。

> **调研备注（2026-08-14 联网核实）**：量子核/feature-map 用于 SSH 拓扑相分类 **无直接先例**。现有最接近的工作：
> - 量子储层计算 QRC（Xin et al. PRA 2025）：量子态直喂储层 + 无监督，走"量子数据"路线，非经典特征→量子核
> - QMC spin-opstring 输入 + 监督 ML（2503.23098）：量子数据做输入，非核方法
> - 非厄米 SSH 用**经典**扩散映射/核（2010.14516）：经典核，且 NHSE 下需去奇异化预处理
> 我们"同一批经典5特征 × 三种引擎（经典核/量子核/量子生成特征）"的控制对比 = 方法论空白。

---

### Step 3：quantum circuit 生成的特征（栏③）— 🔶 取样进行，非全网格

**为什么取样**：DQAP 每点 ~1.4-1.75 hr（qkm_grid_features 烟测 3 点 4.1hr 实测）→ 全 1092 点 ≈ 62-80 天单线程，**不可行**。改取样对比。

**取样协议（2026-08-14 用户定案：主层 U=2 + U=0 干净参照）**：
- 主层：**U=2**（M=3 精确实证，Q=1.0）
- 干净参照：**U=0**（spinless 切片，标签最干净、无 Hubbard confound）——与主层**同一批 (t1,t2) 取样坐标**，两层各算一遍 DQAP
- **排除 U=4**（实证 Q=0.17 完全失败，污染主导）
- 取样点数：每层先 **30 点**（共 60 次 DQAP 运行 ≈ 60×1.4hr/8核 ≈ **10.5 hr 一晚**）；CI 不足再补到 60/层
- 平衡：triv/topo 各半（对齐全网格 546/546）
- 加密：t1=t2 对角线临界区附近（相变敏感）+ 两相代表区均匀
- 栏①、② 在**两层全网格**免费给上下文（经典特征 + feature-map 都便宜）；三栏 apples-to-apples 只在取样点子集

> **CI 说明（30 点预算的代价）**：5-fold CV 下 n=30 → 每折测试仅 6 点，精度只能取 6 个离散值。Wilson CI（`binom_ci` 现状）：30/30 全对 → CI=[64%,100%]；25/30 → [67%,100%]。比全网格 [99.6,100] 宽得多。含义：样本层结论是"≥64%（95% CI）"，对击败 null 50% 仍决定性，但对精确量化不够。缓解：a) 60 点 → 60/60 全对 CI=[94%,100%]（每折 12 测）；b) 关键 claim 靠"三栏同点排序 + 相对差异"，不靠单点精度；c) U=0 层 ①② 全网格免费补上下文。成本↔精度权衡，如实报告。

**同点对齐（"取样而不是直接对比"的含义）**：
- 栏③ 只在取样点上跑（贵）
- 栏①、② 在**同一批取样点子集**上评估 → 三栏取样层直接可比
- 栏①、② 另在全网格报告上下文（便宜）
- **不做**全网格三栏并排（栏③ 不可行），而是取样层 apples-to-apples + 全网格上下文

**DQAP 计算**：
- `qkm_grid_features.py`（已有，逐点建线路）→ `--max-pts N` 取样跑，多核 worker 并行（8 核 → 30 点 ≈ 6 hr）
- 每点态到手后：fidelity 特征 / 核 / ⟨Zᵢ⟩ / ⟨nᵢ⟩ / 关联矩阵全免费（"一个态、多次读出"）
- 输出 `qkm_grid_M3.npz` 格式的取样版 → `qkm_ml.py` 读入跑三栏
- 栏③ 内部：量子-only vs 量子+经典（王远峰"optionally + 经典特征"）两小栏

---

## 依赖 / 里程碑

| 里程碑 | 内容 | 交付物 |
|---|---|---|
| M1 | Step 2 脚本落地 + 全网格跑通 | qkm_featuremap 结果：PSD/KTA/消融表 + 决策图数据 |
| M2 | Step 3 取样 30 点 DQAP 跑完 | 取样 qkm_grid npz |
| M3 | 三栏同点消融表 + 对比图 + 一页 md | 交王远峰（格式对齐 report_classical5） |

**顺序**：M1 先动（不依赖 M2、成本低）；M2 尽早并行启动（主时间成本）；M3 收口。

## 变更记录
- 2026-08-14 新建。三栏定义从旧结构（①经典 ②K_DQAP ③hybrid）改为王远峰口径（①经典 ②经典→量子核 embedding ③量子生成特征）；旧 hybrid 折叠进栏③"optionally + 经典特征"。
- 2026-08-14 用户要求"先对比再动手"：新增「与旧方案对比」节（旧②→新③读出、新②全新、旧③核级→新③特征级语义变化）；联网调研六路文献（Havlíček 2019 / Bowles 2024 / Huang-Preskill PRL 2021 / Huang-McClean NC 2021 / Thanasilp 2024 / SSH-QML 先例检索），Step 2 加入 geometric difference + 核平凡化诊断、诚实 framing 修正（"970"→Bowles 实际口径 12 模型×160 数据集）、确认"SSH 量子核分类"文献空白；Step 3 取样定案 U=2 主层 + U=0 干净参照（同坐标各 30 点）+ CI 代价说明。
